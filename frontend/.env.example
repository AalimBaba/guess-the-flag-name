# URL of the deployed backend API, including the /api suffix.
# Used at build time by Vite (see vite.config.js) — set this as a repository
# variable named VITE_API_URL so the GitHub Pages build picks it up.
VITE_API_URL=https://your-backend-host.example.com/api
