# Optional URL of the deployed backend API, including the /api suffix.
# Leave this unset for the guest-only GitHub Pages deployment. When a backend
# is available, set a repository variable named VITE_API_URL.
VITE_API_URL=https://your-backend-host.example.com/api
